---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gamers                  # Ingress name
  labels:
    app: gamers
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "gamers.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gamers               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
